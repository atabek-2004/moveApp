import 'package:flutter/material.dart';

class ListDeliversWidget extends StatelessWidget {
  const ListDeliversWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}